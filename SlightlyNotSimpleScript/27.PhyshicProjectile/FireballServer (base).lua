local RunS = game:GetService("RunService")
local firebal = game.ReplicatedStorage:WaitForChild("Fireball")
local projectile = game.ServerStorage.cannonball

local SPEED = 100
local GRAVITY_M = 0.25
local MAC_DIS = 250

firebal.OnServerEvent:Connect(function(plr, mousePos)
	-- setup

	-- create projectile

	-- update movement per frame
	
end)
